apiVersion: v1
data:
  routingdburl: "{{ .Values.datasources.dburl }}"
  rabbitmqhost: "{{ .Values.datasources.mqhost }}"
  rabbitmqport: "{{ .Values.datasources.mqport }}" 
kind: ConfigMap
metadata:
  name: routing-config